###########################################
# Colab-safe runtime requirements (no Jupyter)
# - Avoids upgrading Colab's pinned Jupyter stack
# - Polars pinned to a range compatible with RAPIDS bridges
###########################################

# Core data and plotting
numpy>=1.26
pandas>=2.2
polars>=1.25,<1.29
pyarrow>=14
matplotlib>=3.8
seaborn>=0.13

# Modeling, stats, and diagnostics
scipy>=1.11
statsmodels>=0.14
patsy>=0.5
scikit-learn>=1.4

# Explainability
shap>=0.45
xgboost>=2.0  # optional; SHAP falls back to RandomForest if absent
tabulate>=0.9  # optional; pretty tables for artifacts

# NLP-based gender inference
gender-guesser>=0.4

# NOTE:
# - Do not include/ipgrade Jupyter bits (ipykernel, ipython, jupyter-*, ipywidgets) in Colab.
# - If you need a local dev environment (JupyterLab, widgets, etc.),
#   install from requirements-dev.txt in this folder.
# - GPU libs like torch are intentionally excluded for Colab stability.
